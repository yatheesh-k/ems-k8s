pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "pipeline is working"'
                sh 'echo "trigger status : green "'
                sh 'echo "trigger without SCM 3"'
                
            }
        }
    }
}
