services:
  employee-images:
    image: employee-images:latest
    ports:
      - "1000:1000"
    networks:
      - my_network
  identity-image:
    image: identity-image:latest
    ports:
      - "2000:2000"
    networks:
      - my_network
  ui-image:
    image: ui-image:latest
    ports:
      - "3000:3000"
    networks:
      - my_network
  invoice-image:  # Corrected indentation here
    image: invoice-image:latest
    ports:
      - "4000:4000"
    networks:
      - my_network
networks:
  my_network:
    driver: bridge

