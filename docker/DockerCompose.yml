version: '3.8'

services:
  frontend:
    build:
      context: /var/lib/jenkins/workspace/ems-deployement/ui/
      dockerfile: docker/FrontendDockerfile
    ports:
      - "3000:3000"
    networks:
      - my-network

  identity:
    build:
      context: /var/lib/jenkins/workspace/ems-deployement/identity/
      dockerfile: docker/IdentityModuleDockerfile
    ports:
      - "9090:2800"
    networks:
      - my-network

  employee:
    build:
      context: /var/lib/jenkins/workspace/ems-deployement/employee
      dockerfile: docker/EmployeeModuleDockerfile
    ports:
      - "8092:8092"
    networks:
      - my-network

networks:
  my-network:
    external: true
